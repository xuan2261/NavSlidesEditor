# Adversarial PPTX fixtures

Synthetic, project-owned edge packages for the **adversarial lane only**.

- Do **not** add these to metrics averages (`test:pptx:corpus-metrics`).
- Run: `npm run test:pptx:adversarial`
- Rebuild: `node server/services/pptx-import/pptx-import-adversarial-suite.js --materialize`

See parent [README](../README.md) for the expected reject/map table and CRC policy.
